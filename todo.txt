6) add copied popup for solutions---
16) align cube center when i vertically shrink

end of project: 1) understand all TODOS in code, clean up comments and tim's variable names

ask TM:
         x) script tag on cube ASKED ON SO
         x) center navbar when thin window-----
         x) hiding tooltip not working-----
         x) add click-through dragging on drag to view
         x) is it okay to keep all icons downloaded in font awesome or only ones we need-----
         x) how to sort solutions list because of the deisgn choices, should only appear when solutions exist

ask TIM: 3) add cancel search
         4) add server side and client side validation
         x) create generating solutions loading icon in solutionsDisplay

I can do:
          x) add click animation for submit button-----

after flex styling:
                    x) add styling for different screen sizes
